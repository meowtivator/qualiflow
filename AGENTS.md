# QualiFlow Agent Guide

이 파일은 Codex와 Claude Code 공용 규칙입니다. (CLAUDE.md는 이 파일을 import만 합니다 — 규칙 수정은 여기서만.)

## 레포 구조

- `apps/web` — Next.js App Router 데모 앱
- `packages/core` — 도메인 공개 계약 (Lead/Thread/Message/Qualification 타입, ConversationAdapter 인터페이스)
- `packages/adapter-*` — 채널별 어댑터 (mock, alibaba)
- `supabase/migrations` — DB 스키마 + RLS 정책

검증 명령: `pnpm run verify` (typecheck + lint + build). 로컬 실행: `pnpm run dev` → http://localhost:3000. 로컬 인증 메일은 Mailpit(http://127.0.0.1:54324)에서 확인.

## 소유 경계 — 에이전트가 임의로 바꾸지 않는 영역

이 레포의 소유자는 아래 네 영역을 직접 이해하고 결정합니다. 에이전트는 이 영역에 닿는 변경을 **스스로 진행하지 말고 멈춰서 보고**합니다.

1. **공개 계약**: `packages/core`의 타입과 인터페이스. 변경이 필요해 보이면 코드를 쓰기 전에 멈추고 "무엇을 왜 바꿔야 하는지"부터 보고.
2. **데이터 모델**: `supabase/migrations`의 테이블/컬럼/RLS. 스키마 변경이 필요하면 마이그레이션 작성 전에 먼저 보고.
3. **보안 경계**: `apps/web/src/lib/supabase/`, `apps/web/src/app/auth/`, `apps/web/src/proxy.ts`, 시크릿과 환경변수. 이 영역을 수정할 때는 변경 내용을 줄 단위로 평이한 한국어로 설명.
4. **비즈니스 규칙**: 등급 판정, lifecycle 전환, 기본값, 자동 분기 같은 임의 규칙. 새로 만들 때는 코드에 숨기지 말고 보고서에 명시.

## 작업 규칙 (사전 제약)

- **요구받지 않은 기능을 "하는 김에" 추가하지 않는다.** 좋은 아이디어가 떠오르면 구현하지 말고 제안만 한다.
- 시크릿은 `apps/web/.env.local`에만. 코드·커밋·로그에 절대 금지. `NEXT_PUBLIC_` 접두사가 붙은 값은 브라우저에 노출된다는 뜻임을 항상 의식한다.
- 외부 API(Alibaba 등) 연동 코드는 서버 사이드(route handler / server action)에만 둔다. 클라이언트 컴포넌트("use client")에 토큰이 들어가면 안 된다.
- 커밋은 기능 단위로 작게. 메시지는 기존 컨벤션(`feat:`, `chore:`)을 따른다.
- 소유자는 비개발자와 개발자 사이의 학습자다. 설명은 전문용어를 풀어서, 구체적으로.

## 변경 후 보고 형식 (필수)

코드를 변경한 모든 작업은 응답 마지막에 아래 보고를 **한국어로** 붙인다. 항목을 생략하지 않는다 — 해당 없으면 "없음"이라고 쓴다.

### 변경 보고

**1. 스코프**
변경된 파일 전부를 나열하고 각각 한 줄로 "왜 바뀌었는지". 요구받지 않았는데 바뀐 것이 있으면 맨 위에 `⚠️ 요구 외 변경:`으로 따로 표시. (없으면 "요구 외 변경 없음")

**2. 트레이스**
이 변경이 만들거나 바꾼 동작 하나를 사용자 행동 기준으로 추적: 시작점(클릭/요청)부터 최종 결과까지, 경유하는 파일 순서대로 한 단계에 한 줄.

**3. 경계 스캔** — 아래 4개 각각에 "없음" 또는 정확한 위치+내용:
- 공개 계약(`packages/core`) 변경
- DB에 저장되는 데이터 모양 변경
- 시크릿/인증/외부 입력이 지나가는 경로 신설
- 새로 박힌 비즈니스 판단 (기본값, 분기 규칙, 임의로 정한 동작)

**4. 검증 절차**
소유자가 직접 실행할 수 있는 확인 절차: 명령어, 누를 버튼, 기대 결과. `pnpm run verify` 결과를 포함하되, "테스트 통과했습니다" 같은 자기 보고만으로 끝내지 않는다.

**5. 교차 리뷰 포인트**
이 변경에서 다른 모델(작성자가 아닌)이 집중적으로 의심해야 할 지점 1~2개. 작성자 스스로 가장 자신 없는 곳을 정직하게 지목한다.
