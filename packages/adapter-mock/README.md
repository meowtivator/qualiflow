# @qualiflow/adapter-mock

Development-only mock adapter for QualiFlow.

It implements the same `ConversationAdapter` contract that future WhatsApp, Instagram, Alibaba, and email adapters should follow. The web app can build and review the messenger UI without depending on real third-party API credentials.
