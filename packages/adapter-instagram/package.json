{
  "name": "@qualiflow/adapter-instagram",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Instagram login-session CLI for QualiFlow (apps/web connectors launch가 경로로 spawn). 어댑터 본체는 adapter-chat으로 흡수됨.",
  "scripts": {
    "inbox:login": "tsx ./src/cli/login-session.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "25.9.1",
    "tsx": "4.20.6",
    "typescript": "6.0.3"
  }
}
